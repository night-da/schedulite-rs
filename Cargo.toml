[package]
name = "schedulite-rs"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
threadpool = "1.8"

[[bench]]
name = "pool_vs_spawn"
harness = false
